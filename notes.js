const add = (title, body) => {
    console.log(`Adding note ${title} - ${body}`);
};
const read = (id) => {};
const remove = (id) => {};
const list = (id) => {};

module.exports = {
    add,
    read,
    remove,
    list
}