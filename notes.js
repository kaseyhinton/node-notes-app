const add = (title, body) => {
    console.log(`Adding note ${title} - ${body}`);
};
const read = (title) => {};
const remove = (title) => {};
const list = (title) => {};

module.exports = {
    add,
    read,
    remove,
    list
}